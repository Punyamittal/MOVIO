# movio-indicvoice dependencies (Python 3.10+)
#
# Install base packages:
#   pip install -r requirements.txt
#   # On some Linux/PEP-668 systems you may need:
#   #   pip install --break-system-packages -r requirements.txt
#
# Git installs (run separately after requirements.txt, optional):
#   pip install git+https://github.com/ai4bharat/IndicF5.git
#
# Optional GPU / ASR:
#   # bitsandbytes for attempted INT8 (may not be architecture-compatible)
#   pip install bitsandbytes
#   # IndicWhisper if available; otherwise openai-whisper fallback is listed below

deep-translator>=1.11.4
edge-tts>=6.1.0
imageio-ffmpeg>=0.5.0
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
requests>=2.31.0
python-dotenv>=1.0.0
youtube-transcript-api>=0.6.2
datasets>=2.18.0
indic-nlp-library>=0.92
torch>=2.1.0
# IMPORTANT (Windows): default PyPI torch is often CPU-only (+cpu).
# For GPU TTFA, reinstall CUDA wheels after requirements.txt, e.g.:
#   pip uninstall -y torch
#   pip install torch --index-url https://download.pytorch.org/whl/cu126
soundfile>=0.12.1
numpy>=1.24.0
aiohttp>=3.9.0
httpx>=0.27.0
psutil>=5.9.0
pynvml>=11.5.0
jiwer>=3.0.0
openai-whisper>=20231117
qrcode>=7.4
transformers>=4.38.0
accelerate>=0.27.0
sentencepiece>=0.1.99
protobuf>=4.25.0
websockets>=12.0
pydantic>=2.6.0
# IndicF5: install from git (see header comments)
